# @zelavis/app-auth-username-password

Username/password provider service for `zelavis/app/auth`.

This package registers a `username-password` credential provider and leaves password-hash verification to the consuming application.
