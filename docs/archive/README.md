# Archive

This directory preserves design notes and early implementation documentation that may inform future Zelavis packages.

Archived material is not part of the current public API. Treat it as research input: extract ideas only when they fit the small, framework-agnostic package boundaries described in `AGENTS.md`.
