---
"zelavis": minor
---

Add a scoped Project metadata update route and SDK method, plus a matching CLI
rename command, so clients can manage the canonical Project name without
maintaining a local alias.
