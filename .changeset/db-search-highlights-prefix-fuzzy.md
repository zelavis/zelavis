---
"zelavis": minor
---

Support prefix matching (`comput*` / `prefix: true`), fuzzy matching (`macbok~` / `~N` / `fuzzy: true`), and snippet highlights (`<mark>...</mark>`) in `zelavis/db` search queries.
