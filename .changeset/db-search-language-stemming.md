---
"zelavis": minor
---

Support language-aware stemming (`language: "en"`, English Porter stemmer) in collection analyzers and rich boolean search query expressions (`AND`, `OR`, `NOT`, `-`, `&&`, `||`, `!`), grouping with parentheses `(...)`, and field scoping (`field:term`, `field:"phrase"`, `field:prefix*`, `field:fuzzy~`) in `zelavis/db`.
